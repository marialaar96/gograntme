<!DOCTYPE html>
html(lang="en")
    head
        include components/meta.pug
        meta(property="og:title", content="GoGrantMe")
        meta(property="og:description", content="Easily search and apply for journalism grants all over the world.")
        meta(property="og:url", content="https://gograntme.com")
        meta(property="og:image", content="#")
        meta(name="keywords", content="Journalism, Grants, Grant, Apply")
        include components/general-meta-links.pug
        link(rel="stylesheet", href="styles/index.css")
        title GoGrantMe
    body
        .wrapper
            include components/nav.pug
            .content
                .vertical-center
                    .content-center
                        .greeting
                            h1 Welcome to GoGrantMe!
                            p Here you can find grants for journalism from all over the UK, all in one place. Start your search by typing in a topic, an interest or a city.
                        .form
                            form(role=search)
                                label(for="site-search") What? Where?
                                .input
                                    input(type="search" id="site-search" name="q" aria-label="Search through site content")
                                    button.main(type="submit") 
                                        a(href="grants.html") Go
        
        <!-- The core Firebase JS SDK is always required and must be listed first -->
        script(src="/__/firebase/8.1.2/firebase-app.js")

        <!-- TODO: Add SDKs for Firebase products that you want to use https://firebase.google.com/docs/web/setup#available-libraries -->
        script(src="/__/firebase/8.1.2/firebase-analytics.js")

        <!-- Initialize Firebase -->
        script(src="/__/firebase/init.js?useEmulator=true")

        include components/footer.pug
        include components/firebase-scripts.pug
        script(src="scripts/nav.js")